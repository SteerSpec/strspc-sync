name: SteerSpec Monitor
on:
  schedule:
    - cron: '0 8 * * 1'
  workflow_dispatch:

jobs:
  monitor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: SteerSpec/strspc-sync/monitor@v0.2.0
        env:
          GITHUB_TOKEN: ${{ secrets.STEERSPEC_TOKEN }}
